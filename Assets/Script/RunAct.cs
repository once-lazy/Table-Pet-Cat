using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RunAct : MonoBehaviour
{
    // Start is called before the first frame update
    public void DesAct()
    {
        Destroy(gameObject);
    }
}
